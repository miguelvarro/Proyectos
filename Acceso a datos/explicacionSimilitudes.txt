🧠 Proyecto 1 — Sistema de Similitudes Semánticas + RAG (Ollama + ChromaDB)
1️⃣ Descripción general

Este proyecto implementa un sistema completo de:

Generación de embeddings semánticos usando Ollama (nomic-embed-text)

Almacenamiento vectorial con ChromaDB

Recuperación por similitud (top-k)

Sistema RAG (Retrieval-Augmented Generation)

Re-ranking opcional con LLM

Evaluación automática de calidad (groundedness, claridad, etc.)

Ingesta multi-formato (JSON, TXT, MD, PDF)

CLI profesional con múltiples comandos

Es un pipeline completo de IA semántica de extremo a extremo.

2️⃣ Arquitectura técnica
Usuario CLI
   │
   ▼
main.py  (orquestador)
   │
   ├── src/store.py        → ChromaDB (vector store)
   ├── src/embedder.py     → Ollama embeddings
   ├── Ollama /api/chat    → generación RAG
   └── Evaluación LLM      → scoring automático

3️⃣ Componentes del código
🔹 src/embedder.py

Responsabilidad:

Generar embeddings mediante POST /api/embeddings de Ollama.

Modelo por defecto: nomic-embed-text.

Flujo:

requests.post("http://localhost:11434/api/embeddings")


Devuelve un vector numérico (list[float]) que representa el significado del texto.

🔹 src/store.py — Capa de persistencia vectorial

Responsabilidad:

Gestionar la colección persistente en ChromaDB.

Añadir textos con embeddings.

Consultar por similitud.

Eliminar, resetear, estadísticas.

Métodos clave:

add_text()

Genera embedding.

Inserta en Chroma:

id

document

embedding

metadata

query()

Convierte la consulta en embedding.

Recupera top_k más cercanos (cosine distance).

Permite filtro where por metadatos.

stats()

Cuenta documentos.

Agrupa por autor.

🔹 main.py — Orquestador CLI

Es el núcleo del proyecto. Implementa comandos:

Comando	Función
init	Inicializa colección
add	Inserta texto manual
ingest	Ingesta masiva
query	Búsqueda semántica
rag	RAG completo
train	Entrenamiento intensivo
reset	Resetea colección
4️⃣ Sistema RAG

El flujo RAG implementado es:

Embedding de pregunta

Recuperación top-k

Filtro por:

autor

tag

where JSON

(Opcional) Re-ranking con LLM

Construcción de prompt con contexto

Generación con Ollama

(Opcional) Evaluación automática

5️⃣ Re-ranking LLM

Si se activa --rerank:

Se pasa la pregunta + candidatos al LLM.

El modelo asigna score 0-10.

Se reordenan candidatos.

Se eligen los top_k finales.

Esto mejora relevancia contextual.

6️⃣ Evaluación automática

Con --eval:

Se solicita al modelo que evalúe:

groundedness

completeness

clarity

uses_sources

hallucinations

Devuelve JSON estructurado.

7️⃣ Entrenamiento intensivo

python main.py train

Realiza:

Template automático si frases.json vacío.

Ingesta completa de carpeta data/.

Estadísticas finales.

8️⃣ Nivel técnico alcanzado

Este proyecto ya incluye:

Vector DB real

Embeddings reales

RAG funcional

Re-ranking LLM

Evaluación automática

Filtros estructurados

Persistencia

CLI profesional

Es un proyecto de nivel avanzado / profesional en IA aplicada.