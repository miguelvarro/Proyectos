✅ 1. Comprobaciones iniciales
🔹 Asegúrate de tener:

Python instalado (3.10+ recomendado)

Ollama instalado

Modelos descargados:

ollama list


Si no aparecen:

ollama pull nomic-embed-text
ollama pull llama3.1:8b-instruct-q4_0

✅ 2. Iniciar Ollama

En una terminal:

ollama serve


Debe quedar ejecutándose en:

http://localhost:11434


No cierres esta terminal.

✅ 3. Activar entorno virtual (si usas venv)

Dentro del proyecto:

cd similitudes
venv\Scripts\activate


Si no tienes entorno:

python -m venv venv
venv\Scripts\activate
pip install chromadb requests pypdf

✅ 4. Inicializar colección (si vienes limpio)
python main.py init

✅ 5. Entrenamiento rápido completo
python main.py train


Esto:

Crea template si hace falta

Ingesta data/

Genera embeddings

Muestra stats

✅ 6. Test mínimo funcional
🔎 Query simple
python main.py query --q "RAG" --k 3

🧠 RAG completo
python main.py rag --q "Explica RAG en una frase" --tag rag --k 2 --rerank --eval


Si responde coherente → todo está funcionando.

⚠️ Problemas comunes
Problema	Causa
Connection refused	Ollama no está corriendo
Embedding error	Modelo no descargado
Metadata error	Pasar listas en vez de CSV
Resultados raros	No hiciste reset antes de reentrenar